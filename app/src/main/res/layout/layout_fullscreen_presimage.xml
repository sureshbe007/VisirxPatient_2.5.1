<?xml version="1.0" encoding="utf-8"?>
<FrameLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent">

    <com.visirx.patient.customview.TouchImageView
        android:visibility="gone"
        android:id="@+id/pres_imgDisplay"
        android:layout_width="fill_parent"
        android:layout_height="fill_parent"
        android:src="@drawable/visirx_logo"
        android:scaleType="fitCenter" />

    <ImageView
        android:visibility="gone"
        android:layout_marginBottom="30dp"
        android:id="@+id/presviewer_file_image"
        android:layout_width="100dp"
        android:layout_height="100dp"
        android:layout_gravity="center"
        android:src="@drawable/ic_new_pdf" />

    <Button
        android:visibility="gone"
        android:layout_marginBottom="100dp"
        android:id="@+id/presviewer_file_button"
        android:layout_width="200dp"
        android:layout_height="wrap_content"
        android:layout_gravity="center_horizontal|center_vertical|bottom"
        android:background="@color/colorPrimary"
        android:text="View"
        android:textColor="@color/white"/>


    <ProgressBar
        android:layout_gravity="center_horizontal|center_vertical"
        android:id="@+id/viewpager_prespagerProgress"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_centerInParent="true"
        android:visibility="gone" />

</FrameLayout>
